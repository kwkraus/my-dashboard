/**
 * Settings Tab Types
 * 
 * Defines the available settings tabs for type safety across the settings interface.
 */

/**
 * Union type defining all available settings tabs
 * Used for tab navigation state management and content rendering
 */
export type SettingsTabs = "profile" | "security" | "notifications" | "advanced";